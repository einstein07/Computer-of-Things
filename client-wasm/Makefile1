CC=g++
CFLAGS="-std=c++11"
SOURCES=src/ComputerOfThings.cpp
DEPS=
OBJECTS=$(SOURCES:.cpp=.o)
EXECUTABLE=fft

all:$(EXECUTABLE)
	@echo Make has finished.

$(EXECUTABLE):$(OBJECTS)
	$(CC) $(OBJECTS) -o $@

.cpp.o:
	$(CC) -c $*.cpp

clean:
	$(RM) *.o *$(EXECUTABLE)
